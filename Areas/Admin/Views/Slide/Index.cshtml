@model List<Slide>

@{
    int count = 0;
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Slides</h4>

                <div class="table-responsive">
                    <a asp-action="create" class="btn btn-success">Create slide</a>

                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>
                                    No
                                </th>
                                <th>
                                    Image
                                </th>
                                <th>
                                    Order
                                </th>
                                <th>
                                    Title
                                </th>
                                <th>
                                    Details
                                </th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (Slide slide in Model)
                            {
                                count++;
                                <tr>
                                    <td class="py-1">
                                        @count
                                    </td>
                                    <td class="py-1">
                                        <img src="/assets/images/website-images/@slide.Image" />
                                    </td>
                                    <td>
                                        @slide.Title
                                    </td>
                                    <td>
                                        @slide.Order
                                    </td>
                                    <td>
                                        <a asp-action="update" asp-route-id="@slide.Id" class="btn btn-warning"><i class="mdi mdi-grease-pencil"></i></a>
                                        <a asp-action="delete" asp-route-id="@slide.Id" class="btn btn-danger"><i class="mdi mdi-delete"></i></a>
                                        <a asp-action="details" asp-route-id="@slide.Id" class="btn btn-primary"><i class="mdi mdi-information-outline"></i></a>
                                    </td>

                                </tr>
                            }


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>